# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# for testing website locally
gem "github-pages", group: :jekyll_plugins

# for checking broken links
gem 'html-proofer'
